query CountriesList($code: ID!){
    continent(code: $code){
        name
        countries
        {
            name
            code
            states{
                name
            }
        }


    }
}