query CountryDetails($code: ID!){
    country(code: $code)
        {
            name
            code
            native
            capital
            states{
                name
            }
        }

}